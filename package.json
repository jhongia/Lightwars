{
  "name": "Lightwars",
  "version": "1.0.0",
  "description": "A game project",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1"
  },
  "dependencies": {
    "express": "^4.22.1",
    "mongoose": "latest",
    "dotenv": "latest"
  }
}
