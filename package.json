{
  "name": "Lightwars",
  "version": "1.0.0",
  "description": "A game project",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "latest",
    "dotenv": "latest"
  }
}
